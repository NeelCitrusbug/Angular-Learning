import { HighlightElDirective } from './highlight-el.directive';

describe('HighlightElDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightElDirective();
    expect(directive).toBeTruthy();
  });
});
